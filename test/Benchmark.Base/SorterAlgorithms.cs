namespace Benchmark.Base
{
    public enum SorterAlgorithms
    {
        Enum,
        Quick,
        Merge,
        Insert,
        Bubble,
        Select,
        Heap,
        Shell,
        Array,
        Linq
    }
}
